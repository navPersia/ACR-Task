version: v1.1.0
steps:
  - build: -t pwsh-purge-task:latest .
    workingDirectory: /workspace
  # Run the script using the built image (CMD runs purge-images.ps1)
  - cmd: pwsh-purge-task:latest
    pull: false
  # /workspace = where ACR mounts your --context
