# task.yml
version: v1.1.0
steps:
  # Run a container and execute your script from the uploaded context
  - cmd: mcr.microsoft.com/powershell:latest -File https://github.com/navPersia/ACR-Task.git
    pull: true
    workingDirectory: /workspace    # /workspace = where ACR mounts your --context
